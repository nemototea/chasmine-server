FROM node:24-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm ci

# Copy source code
COPY . .

# Expose port
EXPOSE 3000

# Run in development mode with Vite dev server
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
